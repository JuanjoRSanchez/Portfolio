/*
import { useHref } from 'react-router-dom';


export default function TextInputWithFocusButton() {
    const inputEl = useHref(null);
    const onButtonClick = () => {
      inputEl.current.focus();
    };
    return (
      <>
        <button onClick={onButtonClick} ref={inputEl}>Focus the input</button>
      </>
    );
  }
  */